---

---

<div class="distance">
  <p>3.2km</p>
</div>

<style>
  .distance {
    display: flex;
    padding: 4px 10px;
    border-radius: 4px;
    justify-content: center;
    align-items: center;

    background-color: var(--ams-color-blue-light);
  }
  .distance p {
    font-size: var(--ams-font-size-body-extra-small);
    color: var(--ams-color-blue);
    font-weight: var(--ams-font-weight-semibold);
  }
</style>
